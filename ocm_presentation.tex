% OCM Dataset Integration — LaTeX Beamer Presentation
% Compile with: pdflatex ocm_presentation.tex
% Requires: beamer, booktabs, graphicx, xcolor, tikz, fontenc, inputenc
%
% Structure: 12 slides mirroring the 8-chapter walkthrough notebook narrative.

\documentclass[aspectratio=169,12pt]{beamer}

% ── Theme & Colours ────────────────────────────────────────────────────────────
\usetheme{default}
\usecolortheme{default}

\definecolor{navyblue}{RGB}{15,30,66}
\definecolor{accent}{RGB}{0,120,200}
\definecolor{lightgray}{RGB}{245,246,248}
\definecolor{darkgray}{RGB}{80,90,110}
\definecolor{green}{RGB}{16,185,129}
\definecolor{red}{RGB}{220,60,60}
\definecolor{amber}{RGB}{245,158,11}

\setbeamercolor{frametitle}{bg=navyblue, fg=white}
\setbeamercolor{title}{fg=white}
\setbeamercolor{subtitle}{fg=white!80}
\setbeamercolor{author}{fg=white!90}
\setbeamercolor{date}{fg=white!70}
\setbeamercolor{institute}{fg=white!80}
\setbeamercolor{structure}{fg=accent}
\setbeamercolor{normal text}{fg=navyblue}
\setbeamercolor{block title}{bg=accent, fg=white}
\setbeamercolor{block body}{bg=lightgray}
\setbeamercolor{alerted text}{fg=accent}
\setbeamercolor{background canvas}{bg=white}

\setbeamerfont{frametitle}{size=\large, series=\bfseries}
\setbeamerfont{title}{size=\LARGE, series=\bfseries}
\setbeamerfont{subtitle}{size=\normalsize}

\setbeamertemplate{navigation symbols}{}

\setbeamertemplate{footline}{%
  \leavevmode
  \hbox{%
    \begin{beamercolorbox}[wd=\paperwidth,ht=2.4ex,dp=1ex,leftskip=4mm,rightskip=4mm]{frametitle}%
      \color{white!70}\footnotesize
      OCM Dataset Integration \hfill
      \insertframenumber{} / \inserttotalframenumber
    \end{beamercolorbox}%
  }%
}

\setbeamertemplate{title page}{%
  \begin{tikzpicture}[remember picture, overlay]
    \fill[navyblue] (current page.south west) rectangle (current page.north east);
    \node[anchor=center] at (current page.center) {%
      \begin{minipage}{0.85\paperwidth}
        \centering
        \color{white!60}\small JAIST — Taniike Lab\par\vspace{6pt}
        {\usebeamerfont{title}\usebeamercolor[fg]{title}\inserttitle}\par\vspace{8pt}
        {\usebeamerfont{subtitle}\usebeamercolor[fg]{subtitle}\insertsubtitle}\par\vspace{20pt}
        \color{white!50}\rule{0.4\paperwidth}{0.4pt}\par\vspace{12pt}
        {\usebeamerfont{author}\usebeamercolor[fg]{author}\insertauthor}\par\vspace{4pt}
        {\small\usebeamercolor[fg]{date}\insertdate}
      \end{minipage}
    };
  \end{tikzpicture}%
}

% ── Packages ───────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, fit, backgrounds}
\usepackage{array}
\usepackage{colortbl}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{amsmath}

% Convenience: coloured badge
\newcommand{\badge}[2]{%
  \colorbox{#1}{\color{white}\footnotesize\bfseries\,#2\,}}

% ── Meta ───────────────────────────────────────────────────────────────────────
\title{Integrating OCM Literature Data\\into Lab-Scale ML Models}
\subtitle{A Walkthrough of Five Transfer-Learning Approaches}
\author{Partha Nupam Nagar}
\date{June 2026}
\institute{JAIST — Taniike Lab}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}
% ══════════════════════════════════════════════════════════════════════════════

% ── Slide 1 — Title ───────────────────────────────────────────────────────────
\begin{frame}[plain]
  \titlepage
\end{frame}

% ── Slide 2 — Chapter 1: The Problem ──────────────────────────────────────────
\begin{frame}{The Problem — Two Datasets, Three Systematic Differences}

  \begin{columns}[T]

    \begin{column}{0.45\textwidth}
      \begin{block}{Our Lab Data}
        \begin{tabular}{ll}
          \toprule
          \textbf{Samples}     & 89{,}074 \\
          \textbf{Year}        & 2025 \\
          \textbf{Preparation} & Impregnation only \\
          \textbf{Mean Y(C2)}  & 5.25\% \\
          \textbf{Conditions}  & Fixed, one lab \\
          \bottomrule
        \end{tabular}
      \end{block}
    \end{column}

    \begin{column}{0.45\textwidth}
      \begin{block}{Published Literature ($\leq$2019)}
        \begin{tabular}{ll}
          \toprule
          \textbf{Samples}     & 3{,}852 \\
          \textbf{Year}        & 1982–2019 \\
          \textbf{Preparation} & 15+ methods \\
          \textbf{Mean Y(C2)}  & 8.67\% \\
          \textbf{Conditions}  & 40 years of diverse labs \\
          \bottomrule
        \end{tabular}
      \end{block}
    \end{column}

  \end{columns}

  \vspace{6pt}
  \textbf{Central question:} Can we use literature to improve our model
  \emph{without hurting it}? Three separate problems make this non-trivial:

  \vspace{4pt}
  \small
  \begin{itemize}
    \item \badge{red}{1.\ Label shift} \ Literature mean is 3.42 pp higher
      ($t = -32.2$, $p < 10^{-200}$) — a systematic offset from publication
      bias and optimised operating conditions (Fanelli, 2012, \textit{PLOS ONE}).
    \item \badge{red}{2.\ Covariate shift} \ 78.5\% of literature samples
      describe chemistry our lab has never tested
      (Pan \& Yang, 2010, \textit{IEEE TKDE}).
    \item \badge{red}{3.\ Publication bias} \ Literature is \emph{skewed}, not
      just shifted — failed experiments are rarely reported, over-weighting
      high yields and distorting the distribution shape.
  \end{itemize}

  \vspace{4pt}
  \colorbox{lightgray}{\parbox{0.93\linewidth}{\scriptsize
    \textbf{Why ML?} The catalyst search space (65 elements $\times$ varying
    loadings $\times$ temperature) is too large to screen physically.
    A surrogate model guides experiments toward high-yield compositions
    (Lookman et al., 2019, \textit{npj Comput.\ Mater.}).
  }}

\end{frame}

% ── Slide 3 — Chapter 2: Visual evidence ──────────────────────────────────────
\begin{frame}{Visualising the Gap — Label Shift, Covariate Shift, Chemistry}

  \begin{columns}[T]

    \begin{column}{0.62\textwidth}
      \includegraphics[width=\linewidth]{fig_walkthrough_label_pca.png}

      \vspace{6pt}
      \includegraphics[width=\linewidth]{fig_element_usage.png}
    \end{column}

    \begin{column}{0.34\textwidth}
      \vspace{6pt}
      \textbf{Top: label \& covariate shift}
      \begin{itemize}\small
        \item Red band = 3.42 pp mean gap
        \item Orange tail = publication-bias skew
        \item PCA: orange spreads outside the blue cluster
      \end{itemize}

      \vspace{6pt}
      \textbf{Bottom: which elements differ?}
      \begin{itemize}\small
        \item Our palette and literature's overlap only partially
        \item Different active phases and promoters appear
        \item Concrete chemistry, not abstract distance
      \end{itemize}

      \vspace{6pt}
      \colorbox{lightgray}{\parbox{\linewidth}{\scriptsize
        These three figures jointly forecast that naive concatenation
        will harm the model — confirmed in slide 6.
      }}
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 4 — Chapter 3: How We Measure Success ───────────────────────────────
\begin{frame}{How We Measure Success — Asymmetric 5-Fold CV}

  \begin{columns}[T]

    \begin{column}{0.55\textwidth}
      \vspace{6pt}
      \textbf{Validation rule}
      \begin{itemize}
        \item Our 89,074 samples split into 5 folds
        \item Training fold = 4/5 of \emph{our} data $+$ any literature
        \item \alert{Validation fold = 1/5 of \emph{our} data only}
        \item Literature \emph{never} appears in validation
      \end{itemize}

      \vspace{8pt}
      \textbf{Why asymmetric?}
      \begin{itemize}
        \item The question is: ``how well does the model predict
          \emph{our} experiments?''
        \item Mixing literature into validation would dilute the answer
        \item Same CV harness is used for all 5 methods — only the
          training data changes
      \end{itemize}
    \end{column}

    \begin{column}{0.42\textwidth}
      \begin{block}{Metric}
        \textbf{RMSE} in units of percentage points of C$_2$ yield.\\[4pt]
        \footnotesize An RMSE of 2.1 means typical error $\approx$ 2 pp of yield.
      \end{block}

      \vspace{6pt}
      \begin{tikzpicture}
        \node[draw=navyblue, rounded corners=3pt, fill=lightgray,
              text width=0.9\linewidth, align=center, font=\small]
          {\textbf{Fold $k$:} \\
           train on $\{\text{4 our folds}\} + L_{\mathrm{extra}}$
           $\rightarrow$ predict on $\text{our fold }k$};
      \end{tikzpicture}
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 5 — Chapter 4: Baseline + Naive Merge ───────────────────────────────
\begin{frame}{Baseline + Why Naive Merging Fails}

  \begin{columns}[T]

    \begin{column}{0.48\textwidth}
      \textbf{Step 1 — establish baseline}
      \begin{tabular}{lr}
        \toprule
        Method & CV RMSE \\
        \midrule
        Baseline (our data only) & \textbf{2.133} \\
        \bottomrule
      \end{tabular}
      \vspace{6pt}\\
      \footnotesize Number every method must beat.

      \vspace{14pt}
      \textbf{Step 2 — try the obvious}
      \begin{tabular}{lr}
        \toprule
        Method & CV RMSE \\
        \midrule
        Baseline & 2.133 \\
        \rowcolor{red!15}
        Naive merge (3{,}852 lit rows) & \textbf{2.248} \\
        \bottomrule
      \end{tabular}
      \vspace{4pt}\\
      \textcolor{red}{$+5.4\%$ worse than baseline.}
    \end{column}

    \begin{column}{0.48\textwidth}
      \begin{block}{Why did it fail?}
        \small
        Model sees two catalysts with nearly identical composition:
        \begin{itemize}\small
          \item One labelled \textbf{5.2\%} (our lab)
          \item One labelled \textbf{8.7\%} (literature)
        \end{itemize}
        No feature explains the gap — flow rate, gas ratio, pressure
        are absent. The model hedges and predicts both badly.
      \end{block}

      \vspace{6pt}
      \colorbox{lightgray}{\parbox{\linewidth}{\small
        The 3.42 pp offset is a \emph{systematic signal} the model
        cannot account for and incorrectly tries to fit.\\[2pt]
        Ben-David et al.\ (2010, \textit{Mach.\ Learn.}): when source
        and target distributions differ significantly, naive combination
        can perform \emph{worse} than target-only training.\\[4pt]
        $\Rightarrow$ \textbf{We need to be much more selective.}
      }}
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 6 — Chapter 5: DRST ─────────────────────────────────────────────────
\begin{frame}{DRST — Filtering by Chemical Similarity}

  \begin{columns}[T]

    \begin{column}{0.52\textwidth}
      \includegraphics[width=\linewidth]{fig_drst_scores.png}

      \vspace{4pt}
      \footnotesize
      Histogram of $P(\text{ours} \mid x)$ for every literature sample.
      Dashed lines = candidate thresholds.
    \end{column}

    \begin{column}{0.44\textwidth}
      \textbf{How it works}
      \begin{enumerate}\small
        \item Train \textit{logistic classifier}
          (finds a boundary in element-space):\\
          our $=1$, literature $=0$
        \item Score each lit sample: $P(\text{ours} \mid x)$
        \item Keep only samples scoring $\geq \tau$
      \end{enumerate}

      \vspace{4pt}
      \textbf{Threshold sweep} (Stone 1974 CV selection)
      \begin{tabular}{lcr}
        \toprule
        $\tau$ & kept & CV RMSE \\
        \midrule
        0.10 & 1168 & 2.068 \\
        0.20 &  987 & 2.034 \\
        \rowcolor{green!15}
        \textbf{0.30} & \textbf{782} & \textbf{2.019} \\
        0.40 &  543 & 2.045 \\
        \bottomrule
      \end{tabular}

      \vspace{4pt}
      \colorbox{lightgray}{\parbox{\linewidth}{\scriptsize
        \textbf{Literature:} Density-ratio selective transfer —
        Huang et al.\ (2006, \textit{NeurIPS}); Sugiyama et al.\
        (2007, \textit{JMLR}).\\[2pt]
        \textbf{Unsolved:} hard cutoff is arbitrary — 0.29 is
        fully discarded; 0.31 gets full weight.
      }}
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 7 — Chapter 6: KMM ──────────────────────────────────────────────────
\begin{frame}{KMM — Soft Weights Instead of a Hard Filter}

  \begin{columns}[T]

    \begin{column}{0.55\textwidth}
      \includegraphics[width=\linewidth]{fig_kmm_weights.png}
    \end{column}

    \begin{column}{0.42\textwidth}
      \textbf{How it works}
      \begin{itemize}\small
        \item Each lit sample gets a weight $w_i \in [0, 10]$
        \item \textit{RBF kernel} measures chemical similarity:
          $K(x,x')=\exp\!\bigl(-\|x-x'\|^2/2\sigma^2\bigr)$
        \item Solve QP: $\min \frac{1}{2} w^\top K_{ss} w - \kappa^\top w$
        \item $\sigma$ via median heuristic (data-driven, no tuning)
      \end{itemize}

      \vspace{4pt}
      \textbf{Result}
      \begin{tabular}{lr}
        \toprule
        Metric & Value \\
        \midrule
        KMM CV RMSE & \textbf{2.035} \\
        $\Delta$ vs baseline & \textcolor{darkgray}{$-4.6\%$} \\
        Near-zero weights & 78.5\% \\
        Agreement w/ DRST & \textbf{$r = 0.79$} \\
        \bottomrule
      \end{tabular}

      \vspace{4pt}
      \colorbox{lightgray}{\parbox{\linewidth}{\scriptsize
        \textbf{Literature:} Huang et al.\ (2006, \textit{NeurIPS});
        Gretton et al.\ (2009, \textit{Dataset Shift in ML}).\\[2pt]
        \textbf{Reassuring:} two unrelated methods flag the same 78.5\%
        of samples $\Rightarrow$ real signal, not artifact.\\[2pt]
        \textbf{Still unsolved:} literature labels remain in training,
        so the 3.42 pp offset still bleeds in.
      }}
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 8 — Chapter 7: Prior Feature Transfer Pipeline ──────────────────────
\begin{frame}{Prior Feature Transfer — The Winning Pipeline}

  \begin{tikzpicture}[
      box/.style={rectangle, rounded corners=4pt, draw=navyblue, fill=lightgray,
                  text width=2.6cm, align=center, minimum height=1.0cm, font=\small},
      arrow/.style={-{Stealth}, thick, color=accent},
      label/.style={font=\scriptsize, color=darkgray},
      every node/.style={font=\small}
    ]

    \node[box, fill=accent!15] (lit) {Literature\\3{,}852 samples\\(DRST filtered)};
    \node[box, right=1.4cm of lit, fill=accent!25] (xgb) {Stage 1\\XGBoost\\pre-train};
    \node[box, right=1.4cm of xgb, fill=amber!20] (prior) {\texttt{lit\_prior}\\prediction\\(68th feature)};

    \draw[arrow] (lit) -- (xgb);
    \draw[arrow] (xgb) -- (prior);

    \node[box, below=1.4cm of lit, fill=green!15] (lab) {Our Lab Data\\89{,}074 samples};
    \node[box, right=1.4cm of lab, fill=navyblue!10] (merge) {Append\\68\textsuperscript{th} feature};
    \node[box, right=1.4cm of merge, fill=green!25] (lgbm) {Stage 2\\LightGBM\\\emph{our labels only}};
    \node[box, right=1.4cm of lgbm, fill=navyblue!20] (out) {Y(C$_2$)\%\\prediction};

    \draw[arrow] (lab) -- (merge);
    \draw[arrow] (merge) -- (lgbm);
    \draw[arrow] (lgbm) -- (out);

    \draw[arrow, color=amber] (prior) -- (merge);

    \node[label, above=0.2cm of xgb] {Stage 1 — literature expert};
    \node[label, above=0.2cm of lgbm] {Stage 2 — fine-tune on our labels};

  \end{tikzpicture}

  \vspace{6pt}
  \small
  \textbf{Key property:} Stage 2 \emph{never sees literature labels}.
  The 3.42 pp offset affects the \emph{value} of the 68th feature —
  which Stage 2 can calibrate — not the training loss (which it cannot fix).
  This is \emph{feature-based transfer learning}
  (Pan \& Yang, 2010, \textit{IEEE TKDE}): source knowledge enters as a
  feature rather than as additional training labels.
  Stage 1 uses XGBoost (Chen \& Guestrin, 2016) for conservative behaviour
  on 782 samples; Stage 2 uses LightGBM (Ke et al., 2017) for speed on 89k rows.

\end{frame}

% ── Slide 9 — Chapter 8a: Results ─────────────────────────────────────────────
\begin{frame}{Results — All Five Methods at a Glance}

  \begin{columns}[T]

    \begin{column}{0.55\textwidth}
      \includegraphics[width=\linewidth]{fig_walkthrough_results.png}
    \end{column}

    \begin{column}{0.42\textwidth}
      \vspace{2pt}
      \textbf{Cross-validation (our data)}
      \begin{tabular}{lc}
        \toprule
        Method & CV RMSE \\
        \midrule
        Naive merge        & 2.248 \textcolor{red}{$+5.4\%$} \\
        Baseline           & 2.133 \\
        KMM weighted       & 2.035 \\
        DRST ($\tau=0.30$) & 2.019 \\
        \rowcolor{green!15}
        \textbf{Prior FT}  & \textbf{1.907} \badge{green}{$-10.6\%$} \\
        \bottomrule
      \end{tabular}

      \vspace{10pt}
      \textbf{OOD robustness}
      \begin{tabular}{lc}
        \toprule
         & RMSE \\
        \midrule
        Baseline & 6.53 \\
        \rowcolor{green!15}
        \textbf{Prior FT} & \textbf{3.60} \badge{green}{$-45\%$} \\
        \bottomrule
      \end{tabular}

      \vspace{6pt}
      \scriptsize OOD: 2{,}139 non-Impregnation lit samples, unseen.
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 10 — Chapter 8b: SHAP ───────────────────────────────────────────────
\begin{frame}{SHAP — Did the Model Learn Real Chemistry?}

  \begin{columns}[T]

    \begin{column}{0.50\textwidth}
      \includegraphics[width=\linewidth]{fig_shap_beeswarm.png}
    \end{column}

    \begin{column}{0.46\textwidth}
      \vspace{4pt}
      \textbf{Top findings (3{,}000-sample subsample)}
      \begin{itemize}\small
        \item \textbf{\#1:} \texttt{lit\_prior\_prediction} — transfer
          learning is genuinely used, not dead weight
        \item \textbf{Temperature} — strong positive; higher $T \to$ more C$_2$
        \item \textbf{Ba, Mn, La, Ce} — known OCM active phases / promoters
        \item \textbf{Li, K} — mixed / suppressive
      \end{itemize}

      \vspace{8pt}
      \textbf{Residual pattern (ceiling effect)}
      \begin{tabular}{lc}
        \toprule
        Y(C$_2$) range & RMSE \\
        \midrule
        0–6\%   & 1.43–1.48 \\
        6–10\%  & 1.95 \\
        10–15\% & 2.58 \\
        $>$15\% & 4.70 \quad \badge{red}{$-4.2$ pp bias} \\
        \bottomrule
      \end{tabular}

      \vspace{4pt}
      \scriptsize Root cause: GHSV, CH$_4$/O$_2$ ratio, and pressure
      are absent from the dataset.\\[3pt]
      \textbf{SHAP reference:} Lundberg \& Lee (2017, \textit{NeurIPS});
      Shapley values from cooperative game theory (Shapley, 1953).
    \end{column}

  \end{columns}

\end{frame}

% ── Slide 11 — Limitations & Next Steps ───────────────────────────────────────
\begin{frame}{Limitations \& Next Steps}

  \begin{columns}[T]

    \begin{column}{0.32\textwidth}
      \begin{block}{Option A — \badge{green}{Easy}}
        \textbf{Add reaction conditions}\\[4pt]
        \small
        Include GHSV, CH$_4$/O$_2$, pressure as features.\\[4pt]
        Expected to fix the $>$15\% ceiling effect and reduce
        high-yield RMSE from 4.70 to $\sim$2.5.
      \end{block}
    \end{column}

    \begin{column}{0.32\textwidth}
      \begin{block}{Option B — \badge{amber}{Medium}}
        \textbf{Tune transfer threshold}\\[4pt]
        \small
        Sweep DRST $\tau$ and Stage 1 sample size jointly.\\[4pt]
        Current best: $\tau = 0.30$ (782 samples).\ Grid
        search may recover 0.3–0.5\% additional RMSE.
      \end{block}
    \end{column}

    \begin{column}{0.32\textwidth}
      \begin{block}{Option C — \badge{red}{Hard}}
        \textbf{Neural domain adaptation}\\[4pt]
        \small
        Replace LightGBM with DANN or CORAL.\\[4pt]
        High complexity; only justified after reaction
        conditions are added.
      \end{block}
    \end{column}

  \end{columns}

  \vspace{10pt}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Key Limitations}
      \begin{itemize}\small
        \item Missing GHSV / CH$_4$:O$_2$ / pressure features
        \item Literature has 15+ prep methods — coverage uneven
        \item Publication bias: only successful experiments published
        \item OOD improvement may not generalise to new catalyst families
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{Recommended Priority}
      \begin{enumerate}\small
        \item Collect GHSV + CH$_4$/O$_2$ \badge{green}{highest impact}
        \item Re-run Prior FT with expanded features
        \item Revisit $\tau$ tuning with updated data
      \end{enumerate}
    \end{column}
  \end{columns}

\end{frame}

% ── Slide 12 — Closing ────────────────────────────────────────────────────────
\begin{frame}{Summary}

  \centering
  \vspace{6pt}
  \begin{tikzpicture}
    \node[draw=navyblue, line width=1pt, rounded corners=6pt,
          fill=lightgray, text width=0.85\paperwidth, align=left,
          inner sep=14pt] {%
      \small
      \textbf{What we asked:} Can published OCM literature improve
      a lab-data model without harming it? \\[8pt]
      \textbf{What we learned:}
      \begin{itemize}\small
        \item Naive merging \emph{harms} accuracy (\textcolor{red}{$+5.4\%$ RMSE}).
        \item Selective filtering (DRST / KMM) recovers $\sim$5\% improvement.
        \item Using literature as a \emph{prior feature} (not a label) gives
          the biggest gain: \textcolor{green}{$-10.6\%$ CV RMSE, $-45\%$ OOD RMSE}.
        \item SHAP confirms the model uses real OCM chemistry: temperature,
          Ba, Mn, La, Ce — and the literature prior — drive predictions.
      \end{itemize}
      \textbf{Next step that matters most:} add GHSV and CH$_4$:O$_2$
      to the feature set — the high-yield ceiling effect is data-bound,
      not model-bound.
    };
  \end{tikzpicture}

  \vspace{14pt}
  \textcolor{darkgray}{Thank you — questions welcome.}

\end{frame}

% ══════════════════════════════════════════════════════════════════════════════
\end{document}
